# Knowledge Vault — 环境变量配置
#
# 复制为 .env 文件并按需修改：
#   cp .env.example .env

# 知识库 SQLite 数据库路径（默认：./vault.db）
# KNOWLEDGE_DB_PATH=./vault.db

# 语义搜索嵌入模型（默认：BAAI/bge-small-zh-v1.5）
# 需要先安装 fastembed：pip install fastembed
# EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5
